class request {
  constructor({{params}}) {
    {{constructor}}
  }
 
  body() {
    // Define desired object
    var obj = {{objectBody}}
    // Return it
    return obj
  }
}

module.exports = request