const expect = require('chai').expect
const chai = require('chai')
chai.use(require('chai-json-schema'))
const Request = require('{{pagesPath}}');
{{dataDriven}}

module.exports = () => {
    describe("{{describe}}", () =>  {
        {{testFunc}}
    })
}